<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">  
</head>
<body>
    <div class="container mt-4">
        <h2 class="text-center">Welcome to Your Profile</h2>
        <div class="card text-center">
            <div class="card-header">
                <h3><%= user.username %></h3>
            </div>
            <div class="card-body">
                
                <p class="mt-3">Hello <%= user.username %>! Here are your profile details.</p>
                <div class="mt-4">
                    <h5>Your Details:</h5>
                    <p><strong>Name:</strong> <%= user.username %></p>
                    <p><strong>Email:</strong> <%= user.email %></p>
                    <p><strong>Position:</strong> <%= user.position %></p>
                    <p><strong>Salary:</strong> <%= user.salary %></p>

                    
                </div>
                
            </div>
            
        </div>
    </div>
    <div class="text-center mt-4">
        <a href="/logout" class="btn btn-secondary">Logout</a>
    </div>
</div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script> <!-- Bootstrap JS -->
</body>
</html>
